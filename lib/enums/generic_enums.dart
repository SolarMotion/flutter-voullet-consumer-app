enum StorageEnum { email, userID }

enum GenderEnum { male, female }

enum AlertDialogResultEnum { success, error }

enum FeedbackFormTypeEnum { issue, comment, other }

enum VoucherTypeEnum { voucher, coupon }

enum ButtonTypeEnum { download, redeem }

enum VoucherSortByEnum { nationwide, local }
